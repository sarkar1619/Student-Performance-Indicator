const Navbar = () => {
    return (
        <nav className="flex items-center justify-center w-full flex-wrap bg-teal-500 p-6">
            <span className="font-bold text-white text-2xl tracking-tight">
                Student Performance Indicator
            </span>
        </nav>
    );
};

export default Navbar;
